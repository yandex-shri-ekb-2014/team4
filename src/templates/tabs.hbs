{{#each tabs}}
    <a href="/{{../state.locality}}/{{link}}" class="button tabs__tab tabs__tab_
        {{~#if @first~}}
            left
        {{else}}
            {{~#if @last~}}
                right
            {{~else~}}
                middle
            {{/if}}
        {{/if}}
        {{#if class}}
            {{class}}
        {{/if}}
        {{#ifCond link ../state.tab}}
            button-active
        {{/ifCond}}
    ">{{title}}</a>
{{/each}}
